# Mediawiki UserContributions HeatMap (SVG)
Vercel => CloudFlare

```shell
pnpm i
npx wrangler login
npx wrangler kv namespace create mediawiki-usercontrib-heatmap
pnpm run dev
pnpm run deploy
# set wrangler.jsonc

```